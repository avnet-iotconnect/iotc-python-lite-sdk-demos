# Fabric Accel Demo

This folder contains the workshop demo app for the PolarFire SoC Discovery Kit. Run with root so it can access `/dev/mem`.

```
cd /home/weston/demo/fabric-accel
sudo python3 app.py
```

For the full workshop flow, see `microchip-polarfire-soc-dk/FPGA-ACCEL-WORKSHOP.md`.